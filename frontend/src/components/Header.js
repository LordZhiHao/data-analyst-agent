import React from "react";

const Header = () => {
  return (
    <header className="text-center mb-4">
      <h1>🤖 Vanna SQL Agent Dashboard</h1>
      <p className="lead">
        Ask questions in natural language or write SQL directly to query
        BigQuery
      </p>
    </header>
  );
};

export default Header;
